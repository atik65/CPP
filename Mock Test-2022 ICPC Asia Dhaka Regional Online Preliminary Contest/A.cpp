#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{

    int a, b, c;
    cin >> a >> b >> c;

    printf("Sum of %d, %d and %d is %d\n", a, b, c, (a + b + c));

    return 0;
}